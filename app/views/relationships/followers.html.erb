<div class="container">
  <div class="row">
    <div class="col-sm-12 mx-auto">
      <h2 class="text-center">フォローしてくれてる人</h2>
            <% if @followers.exists? %>
              <% @followers.each do |user| %>
               <%= render "users/index", user: user %>
              <% end %>
            <% else %>
              <p>ユーザーはいません</p>
            <% end %>
          </div>
        </div>
     </div>