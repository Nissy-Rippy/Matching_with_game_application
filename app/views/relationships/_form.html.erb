            <% if user.followed_by?(current_user) %>
            <p><%= link_to "フォロー解除(;>_<;)", user_relationships_path(user.id), method: :delete, remote: true %>
            <% else %>
            <p><%= link_to "フォローする(´∀｀*)" , user_relationships_path(user.id), method: :post, remote: true %></p>
            <% end %>