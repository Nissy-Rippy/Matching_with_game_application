  <div class="container">
    <div clss="row">
      <div class="col-sm-12 mx-auto">
  　　　<%# <card>機能はホームページのコードを利用しています　%>
  　　　<h5 class="mt-2 text-center"><strong>(´・ω・｀)コメントしてね</strong></h5>
               <div class="mx-auto">
                <div class="mb-5">
                <%= attachment_image_tag @post, :image, :fill,400,300, fallback: "no_image.jpg", size: "400x300" %>
                <session class="text-left">
                <div><strong>タイトル：</strong><%= link_to @post.post_title, post_path(@post) %><br/><strong>投稿者:　</strong><%= @post.user.name %> </div>
                <div><strong>内容：</strong><%= @post.description %></div>
                <div class="d-flex flex-row bd-highlight m-1">
                <div id="likes_btn_<%= @post.id %>">
                <%= render "likes/like", post: @post %>
                </div>
                <i class="fas fa-comment-dots" id="post_<%= @post.id %>"></i><%= link_to @post.comments.count, post_path(@post)%><i class="fas fa-tags">
                <% @post.tags.each do |tag| %>
                #<%= tag.name %><br/>
                </session>
                <% end %>
                </i>
                </div>
                <p><small class="text-muted"><%= @post.created_at.strftime("%Y/%m/%d %H:%M") %></small></p>
                </div>
                </div>
        <div class="comment-list mx-auto text-center">
          <%# コメント一覧表示%>
        <%= render "comments/index", comments: @comments %>
          </div>
          <%# コメント送信の部分テンプレート%>
        <div class="comments mb-5">
          <%= render "comments/form", post: @post, comment: @comment %>
        </div>

      </div>
    </div>
  </div>