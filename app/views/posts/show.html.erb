<div class="container">
  <div clss="row">
    <div class="col-sm12">
　　　<%# <card>機能はホームページのコードを利用しています　%>
      <div class="card mb-3">
        <svg class="bd-placeholder-img card-img-top" width="100%" height="180" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="xMidYMid slice" focusable="false" role="img" aria-label="Placeholder: Image cap"><title>Placeholder</title><rect fill="#868e96" width="100%" height="100%"/><text fill="#dee2e6" dy=".3em" x="50%" y="50%">Image cap</text></svg>
        <div class="card-body">
          <h5 class="card-title"><%= @post.post_title %></h5>
          <p class="card-text"><%= @post.description %></p>
          <p class="card-text">
            
            <%# いいね機能の部分テンプレート%>
          <div id="likes_btn_<%= @post.id %>">
          <%= render "likes/like", post: @post %><%= " タグ" %>
          </div>
          <%# この部分はjsファイルで非同期のためにidクラスに@post.idとやることで特定のファイルに適応させています%>
          <div id="post_<%= @post.id %>"><%= @post.comments.count %></div></p>
          <p class="card-text"><small class="text-muted"><%= @post.created_at.strftime("%Y/%m/%d %H:%M") %></small></p>
        </div>
      </div>
             <%# コメント一覧表示%>
      <%= render "comments/index", comments: @comments%>
             <%# コメント送信の部分テンプレート%>
      <div class="comments">  
        <%= render "comments/form", post: @post, comment: @comment %>
      </div>
    </div>
  </div>
</div>