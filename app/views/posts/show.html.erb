  <div class="container">
    <div clss="row">
      <div class="col-sm12 mx-auto">
  　　　<%# <card>機能はホームページのコードを利用しています　%>
  　　　<h5 class="mt-2 mb-5 text-center"><strong>投稿詳細なり!</strong></h5>
        <div class="card mb-3">
           <%= attachment_image_tag @post, :image, fallback: "no_image.jpg" %>
           <div class="card-body">
            <h5 class="card-title"><%= @post.post_title %></h5>
            <p class="card-text"><%= @post.description %></p>
            <p class="card-text">
              <%# いいね機能の部分テンプレート%>
            <div id="likes_btn_<%= @post.id %>">
            <%= render "likes/like", post: @post %>
            </div>
              <%# この部分はjsファイルで非同期のためにidクラスに@post.idとやることで特定のファイルに適応させています,今回はコメント数！%>
            <i class="fas fa-comment-dots" id="post_<%= @post.id %>"></i><%= link_to @post.comments.count, post_path(@post) %><i class="fas fa-tags">
            <% @post.tags.each do |tag| %>
             #<%= tag.name %>
             <% end %>
              </i></p>
           
            <p class="card-text"><small class="text-muted"><%= @post.created_at.strftime("%Y/%m/%d %H:%M") %></small></p>
          </div>
        </div>
        <div class="comment-list mx-auto text-center">
          <%# コメント一覧表示%>
        <%= render "comments/index", comments: @comments %>
          </div>
          <%# コメント送信の部分テンプレート%>
        <div class="comments">
          <%= render "comments/form", post: @post, comment: @comment %>
        </div>
        
      </div>
    </div>
  </div>