  <div class="container">
    <div clss="row">
      <div class="col-sm-12 mx-auto">
  　　　<%# <card>機能はホームページのコードを利用しています　%>
  　　　<h5 class="mt-2 text-center"><strong>(´・ω・｀)コメントしてね</strong></h5>
           <div class="row mb-5 mt-5">
              
            <div class="col-md-12 col-lg-6" style="text-align:center">
              <div style="display: inline-block;max-width:400px">
                <div class="w-25"><%= attachment_image_tag @post, :image, :fill,400,300, fallback: "no_image.jpg", size: "400x300" %></div>
                <session class="text-left">
                  <div>タイトル：<%= link_to @post.post_title, post_path(@post) %><br/>投稿者:　<%= @post.user.name %> </div>
                  <div>内容：<%= @post.description %></div>
                  <div class="d-flex flex-row bd-highlight m-1">
                    <div id="likes_btn_<%= @post.id %>">
                      <%= render "likes/like", post: @post %>
                    </div>
                    <i class="fas fa-comment-dots" id="post_<%= @post.id %>"></i>
                    <%= link_to @post.comments.count, post_path(@post)%>
                    <i class="fas fa-tags">
                    <% @post.tags.each do |tag| %>
                      #<%= tag.name %><br/>
                    <% end %>
                    </i>
                  </div>
                  <p><small class="text-muted"><%= @post.created_at.strftime("%Y/%m/%d %H:%M") %></small></p>
                </session>
              </div>
            </div>
        
          <%# コメント送信の部分テンプレート%>
          <div class="comments mb-5 col-sm-6">
            <%= render "comments/form", post: @post, comment: @comment %>
          </div>
          <%# コメント一覧表示%>
          <div class="comment-list mx-auto text-center col-sm-12">
            <%= render "comments/index", comments: @comments %>
          </div>
      </div>
    </div>
  </div>