<div class="container">
  <div clss="row mx-auto">
    <div class="col-sm-12 mx-auto">
        <h1 class="text-center">フォロー中の方のタイムライン</h1>
         <div class="rom mx-auto">
           <% if @posts.exists? %>
            <%= render "posts/index", posts: @posts %>
           <% else %>
           <h3 class="text-center">フォローしている方がそもそもいないよ。。。＞＜</h3>
           <% end %>
         </div>
    </div>
  </div>
</div>


